/*global define*/

define([
    'jquery',
    'backbone',
], function ($, Backbone) {
    'use strict';

  var TodoRouter = Backbone.Router.extend({
    routes: {
    },

  });

  return TodoRouter;
});